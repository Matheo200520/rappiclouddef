name: CI-CD RappiCloud

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:

  build-and-test:
    name: 🧪 Build & Test Microservicios
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Descargar repositorio
        uses: actions/checkout@v3

      - name: 🧰 Instalar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Instalar dependencias de todos los servicios
        run: |
          for svc in usuarios restaurantes pedidos pagos repartidores notificaciones api-gateway
          do
            cd $svc
            npm install
            cd ..
          done

      - name: 🔍 Análisis ESLint (simulado)
        run: |
          echo "Ejecución simulada de ESLint"

  docker-build:
    name: 🐳 Build Docker Images
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: 📥 Descargar código
        uses: actions/checkout@v3

      - name: 🐳 Configurar Docker
        uses: docker/setup-buildx-action@v2

      - name: 🔨 Construir imágenes Docker de microservicios
        run: |
          for svc in usuarios restaurantes pedidos pagos repartidores notificaciones api-gateway
          do
            echo "Construyendo imagen para $svc"
            docker build -t rappicloud-$svc:latest $svc
          done

  load-testing:
    name: ⚡ Pruebas de carga (K6)
    runs-on: ubuntu-latest
    needs: docker-build

    steps:
      - name: 📥 Descargar código
        uses: actions/checkout@v3

      - name: 🧪 Ejecutar prueba de carga con K6
        uses: grafana/k6-action@v0.2.0
        with:
          filename: tests/k6-stress.js  # 👈 AQUÍ EL CAMBIO CORRECTO

  deploy:
    name: 🚀 Despliegue Simulado
    runs-on: ubuntu-latest
    needs: load-testing

    steps:
      - name: 🚀 Lanzar despliegue (simulado)
        run: |
          echo "Despliegue simulado completado para RappiCloud"
          echo "Todo correcto, listo para producción (simulado)"
